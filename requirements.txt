# Python dependencies for py-recon-basics
# Install all at once: pip install -r requirements.txt

# Domain WHOIS lookups (imported as 'whois' in code, installed as 'python-whois')
python-whois

# HTTP header inspection — added Day 5
# requests
